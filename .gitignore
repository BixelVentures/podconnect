# Secrets / runtime state — never commit
*credentials.json
*state.json

# OS cruft
.DS_Store
*.log

# Go build artifact (the manager binary is built into the add-on image, never committed)
podconnect/manager/podconnect-manager
